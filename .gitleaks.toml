# Minimal gitleaks config: catch common API key patterns and tokens
allowlist = [""]

[[rules]]
description = "AWS Access Key"
regex = "AKIA[0-9A-Z]{16}"

[[rules]]
description = "Generic API key"
regex = "(?i)(api[_-]?key|secret|token|wsk|uid)[\s:=\"']+[A-Za-z0-9_-]{10,}"
